version: 1
applications:
    - frontend:
          phases:
              build:
                  commands:
          artifacts:
              baseDirectory: /dist
              files:
                  - '**/*'
          cache:
              paths: []
      appRoot: website
